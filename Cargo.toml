[workspace]
resolver = "3"
members = [ "scanner","cli", "interpreter", "parser", "evaluator"]

[workspace.lints.clippy]
let_and_return = "allow"

[workspace.dependencies]
log = { version = "0.4", features = ["std"] }
env_logger = { version = "0.11" }
