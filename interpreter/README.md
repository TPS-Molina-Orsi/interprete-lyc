# Interpreter

This is the main library which holds all of the logic to the interpreter, a crate aggregator.

Ideally, each step of the interpreter will have its own crate which will all be put togheter here.
